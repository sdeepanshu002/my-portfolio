const SkillIcon = ({ label }: { label: string }) => (
  <div className="bg-blue-100 dark:bg-blue-800 text-blue-600 dark:text-white px-3 py-1 rounded-full text-sm font-medium">
    {label}
  </div>
);

export default SkillIcon;
